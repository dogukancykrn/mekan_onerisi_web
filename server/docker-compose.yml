version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      JWT_SECRET: mekan-super-secret-docker-2025
      NODE_ENV: production
      FRONTEND_URL: https://mekan-onerisi-web-jxm9.vercel.app
    volumes:
      - sqlite-data:/app/data
    restart: unless-stopped

volumes:
  sqlite-data:
    driver: local